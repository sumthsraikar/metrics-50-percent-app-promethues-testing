# Metrics 50% Demo App

This project demonstrates a simple monitoring setup using:
- Python Prometheus client
- Docker container
- Kubernetes deployment
- Prometheus scraping

The application exposes a custom metric that always shows 50% CPU usage.
